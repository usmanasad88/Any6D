This is a pipeline to track 6DOF pose of known objects in a monocular video. First lets modify the existing Any6D repo as necessary and use the data in /home/mani/Repos/Any6D/demo_data/bottle
This contains glb files for a bottle and a scale, both these objects are in the bottlevid.mp4
The glb files are created by SAM3D Objects. I want an output video file with the 3D mesh files overlayed on the input video with the correct pose and scale. 

This is the response of an AI model about a possible approach:

FoundationPose cannot automatically scale your objects.FoundationPose assumes the CAD model provided is metrically accurate (1:1 with reality). If you feed it a unit-scale GLB (from SAM-3D) but the real object is 10cm tall:RGB-only mode: It will estimate the object is $10\times$ further away to make the pixels line up (Z-axis error).RGB-D mode: The ICP/Refinement step will likely fail or diverge because the depth points from the camera will not match the surface of the giant/tiny mesh.You must solve for scale before (or during) the FoundationPose inference. Since you have a wrist camera (assuming RGB-D), you can automate this.The Automated Scaling & Placement PipelineHere is the robust algorithm to implement "Scale-Aware Asset Injection":Phase 1: The "Scale Solver" (Pre-Pose)Before asking FoundationPose "Where is it?", you must ask "How big is it?"Input: Single RGB-D frame from wrist camera + Unscaled GLB.Step 1: Segment. Use SAM (Segment Anything) to get the 2D binary mask of the object in the wrist camera frame.Step 2: Deproject. Mask the Depth Map. Convert these pixels to a 3D Point Cloud (using camera intrinsics).Filter: Remove outliers (statistical outlier removal) to clean up noise at the edges.Step 3: Compute Physical Extents. Calculate the Oriented Bounding Box (OBB) of this point cloud.Let $D_{real}$ be the diagonal length of this OBB (in meters).Step 4: Compute Mesh Extents. Load your GLB (using trimesh). Calculate its OBB diagonal $D_{mesh}$.Step 5: The Scale Factor. $$S = \frac{D_{real}}{D_{mesh}}$$Output: A temporary, scaled GLB file (e.g., bottle_scaled.glb).Phase 2: 6D Pose EstimationNow you have a metric mesh, FoundationPose will work correctly.Input: RGB-D frame + bottle_scaled.glb.Inference: Run FoundationPose.Output: $T_{cam \to obj}$ (4x4 Transformation Matrix).Phase 3: Isaac Sim Injection (The "Digital Twin" Link)You need to transform the pose from the Camera frame to the Isaac Sim World frame.Equation:$$T_{world \to obj} = T_{world \to robot} \times T_{robot \to wrist} \times T_{wrist \to cam} \times T_{cam \to obj}$$$T_{world \to robot}$: Robot base pose in Isaac Sim.$T_{robot \to wrist}$: Forward Kinematics (from joint angles).$T_{wrist \to cam}$: Hand-eye calibration (fixed static transform).